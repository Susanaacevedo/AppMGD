
<!-- Nuevo Navbar -->

<nav class="navbar navbar-expand-lg navbar-light bg-warning sinstyle fixed-top">

  <div class="gallery">
    <a target="" href="<%= root_url %>">
      <img src="lguantes.png" alt="lguantes" width="30" height="30">
    </a>
    <!-- agregue el sgte div-->
    </div>

  <button class="navbar-toggler" type="button" data-toggle="collapse"
   data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false"
   aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item nav-link"><%= link_to 'Contactame', new_contact_path%></li>
    <% if user_signed_in? %>

      <li class="nav-item nav-link"><%= link_to 'Carro de Compras', detcarros_path%></li>
    <% end %>
    </ul>
      <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
    <% if user_signed_in? %>
      <% if current_user.admin? %>
          <li class="nav-item nav-link"><%= link_to 'Metas', goals_path%></li>
          <li class="nav-item nav-link"><%= link_to 'Productos', productos_path%></li>
          <li class="nav-item nav-link"><%= link_to 'Contactos', contacts_path%></li>
      <% end %>
    <% end %>
  </ul>
    <ul class="navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li class="nav-item nav-link"><a href="#"> Welcome <%= current_user.email %>!</a></li>
<!--        <li><a href="#"> Role <%= current_user.role %>  !!</a></li> -->
        <li class="nav-item nav-link"><%= link_to ' Cerrar sesión', destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li class="nav-item nav-link"><%= link_to 'Inicia sesión', new_user_session_path %></li>
        <li class="nav-item nav-link"><%= link_to 'Regístrate', new_user_registration_path %></li>
      <% end %>
    </ul>
  </div>

</nav>
